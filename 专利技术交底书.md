# 专利技术交底书

## 一种基于多模型辩论协同的学术研究自动化方法及系统

---

## 一、背景技术

### 1.1 现有技术问题

在学术研究辅助领域，现有的AI辅助写作工具存在以下问题：

**1. 创新性与严谨性的矛盾**
- 单一AI模型难以同时保证创新性和学术严谨性
- 高温度生成的内容创意丰富但缺乏逻辑严密性
- 低温度生成的内容严谨但容易陷入保守思维

**2. 缺乏有效的方案验证机制**
- 现有系统无法判断研究方案是否符合SCI期刊标准
- 无法自动识别方案中的逻辑漏洞和论证缺陷
- 缺乏多维度的一致性评估体系

**3. 执行计划与学术方案脱节**
- 学术方案往往缺乏可落地的执行步骤
- 学术语言与工程实现之间存在巨大鸿沟
- 缺乏安全可控的代码执行环境

**4. API成本高昂**
- 高频调用商业LLM API产生高额费用
- 无法利用免费的网页版AI工具
- 缺乏有效的降级和容错机制

### 1.2 现有技术方案

现有的多模型协同方案主要包括：

1. **串联审查模式**：单一模型生成，多个模型串行审查，效率低
2. **投票选举模式**：多个模型生成相同内容，投票选择，无法捕捉认知差异
3. **角色扮演模式**：给不同模型分配固定人设，但缺乏深度交互机制

这些方案均无法实现真正意义上的"辩论式协同"，即模型之间的认知碰撞、观点挑战和动态收敛。

### 1.3 本发明的技术领域

本发明属于人工智能辅助学术研究领域，具体涉及一种基于多模型辩论协同的学术研究自动化方法及系统，尤其适用于SCI论文写作辅助、专利技术方案生成、研究可行性评估等场景。

---

## 二、技术方案（详细）

### 2.1 总体架构

本发明提供一种三模型辩论协同系统，包括：

```
┌─────────────────────────────────────────────────────────────────┐
│                        AutoSynth-Bridge                         │
│                                                                 │
│  ┌───────────────┐    ┌───────────────┐    ┌───────────────┐  │
│  │  GPT Agent    │◄──►│ DebateEngine  │◄──►│ Gemini Agent  │  │
│  │  (学术裁判)   │    │  (辩论引擎)   │    │  (创新发散)   │  │
│  └───────────────┘    └───────────────┘    └───────────────┘  │
│          │                    │                      │          │
│          │              ┌─────┴─────┐                │          │
│          │              │  Convergence │              │          │
│          │              │  Checker     │              │          │
│          │              │  (三段式评分) │              │          │
│          │              └─────┬─────┘                │          │
│          │                    │                      │          │
│          ▼                    ▼                      ▼          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Claude Executor (安全执行器)                 │   │
│  │              - 任务分解  - 安全约束  - 重试机制          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              │                                  │
│                              ▼                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              TrajectoryStore (轨迹存储)                   │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 核心组件详细说明

#### 2.2.1 辩论引擎 (DebateEngine)

辩论引擎是整个系统的核心编排器，负责管理多轮辩论的流程控制。

**核心数据结构**：

```python
class DebateRequest:
    query: str                      # 用户查询/研究主题
    topic: str                       # 辩论主题
    task_type: str = "sci_paper"     # 任务类型
    participants: list[str]          # 参与者列表
    rounds: int = 3                  # 辩论轮数

class DebateRound:
    round_num: int                   # 轮次编号
    participant: str                  # 参与者名称
    content: str                     # 生成内容
    role: DebateRole                 # 角色类型
    model: str                       # 使用的模型
    latency_ms: int                  # 延迟（毫秒）
```

**辩论流程控制算法**：

```python
async def _run_debate_rounds(self, request, result, enable_concurrent):
    # 初始化：第一个模型（Gemini）进行初始发散
    first_content = await first_provider.generate(init_messages)
    rounds.append(DebateRound(round_num=0, participant=first_name, content=first_content))

    # 多轮辩论
    for round_num in range(1, request.rounds + 1):
        if enable_concurrent and len(available_participants) > 1:
            # 并发模式：同一轮次所有参与者同时生成
            round_responses = await self._concurrent_round(...)
            rounds.extend(round_responses)
        else:
            # 串行模式：按顺序生成
            for name, role, provider in available_participants:
                round_resp = await self._single_round(...)
                rounds.append(round_resp)

    return rounds
```

**关键特征**：
- 支持并发和串行两种辩论模式
- 参与者可灵活配置
- 自动处理Provider不可用的情况
- 完整的错误捕获和状态管理

#### 2.2.2 角色系统 (DebateRoles)

定义了五种认知角色，每种角色具有不同的思维模式和输出风格：

```python
class DebateRole(str, Enum):
    ARCHITECT = "architect"      # 架构设计者 - 提出方案
    CRITIC = "critic"           # 风险审查者 - 质疑问题
    EXECUTOR = "executor"       # 落地工程师 - 分解步骤
    RESEARCHER = "researcher"    # 资料分析者 - 补充依据
    JUDGE = "judge"             # 最终裁判 - 综合结论
```

**默认角色映射**：

```python
def get_default_role_map() -> Dict[str, DebateRole]:
    return {
        "gemini": DebateRole.ARCHITECT,  # Gemini负责方案设计
        "gpt": DebateRole.CRITIC          # GPT负责质疑审查
    }
```

**角色Prompt模板**：

```python
# 架构设计者Prompt
ARCHITECT_PROMPT = """
你是一位【架构设计者】，负责：
1. 提出有创新性的方案
2. 设计结构清晰的解决路径
3. 规划可落地的路线图
4. 尽量发散思路，不要保守
请用简洁的语言组织你的方案，开头加上【架构方案】
"""

# 风险审查者Prompt
CRITIC_PROMPT = """
你是一位【风险审查者】，负责：
1. 找出架构中的潜在问题和漏洞
2. 质疑方案的可行性
3. 要求提供证据和依据
4. 提醒风险点和注意事项
请用尖锐但专业的语言指出问题，开头加上【风险审查】
"""
```

#### 2.2.3 三段式收敛检查器 (ConvergenceChecker)

这是本发明的核心创新点之一，实现了多维度的一致性评估：

**第一段：语义相似度计算**

```python
class ConvergenceChecker:
    def calculate_similarity(self, text1: str, text2: str) -> float:
        """使用SentenceTransformer计算语义余弦相似度"""
        if not self.embedding_model:
            return 0.5  # 默认中间值
        
        emb1 = self.embedding_model.encode(text1, convert_to_tensor=True)
        emb2 = self.embedding_model.encode(text2, convert_to_tensor=True)
        return float(util.cos_sim(emb1, emb2).item())
```

**第二段：结构化合意评分**

```python
class StructuredAgreementScore(BaseModel):
    innovation_aligned: bool       # 创新点是否一致
    evidence_aligned: bool         # 证据/实验设计是否一致
    patent_scope_aligned: bool     # 专利保护范围是否一致
    implementation_aligned: bool  # 实现路径是否一致

    def compute(self) -> float:
        aligns = [
            self.innovation_aligned,
            self.evidence_aligned,
            self.patent_scope_aligned,
            self.implementation_aligned
        ]
        self.score = sum(aligns) / len(aligns)
        return self.score
```

关键问题关键词定义：

```python
KEYWORDS = {
    "innovation": ["创新", "novel", "创新点", "突破", "首次", "新方法"],
    "evidence": ["实验", "数据", "验证", "证明", "实验设计", "ablation", "dataset"],
    "patent_scope": ["专利", "保护", "权利要求", "实施例", "覆盖范围"],
    "implementation": ["代码", "实现", "算法", "架构", "模块", "接口"],
    "sci_level": ["SCI", "二区", "发表", "论文", "期刊", "IF", "impact factor"],
}
```

**第三段：风险消解评分**

```python
class RiskResolutionScore(BaseModel):
    sci_feasibility: bool = False      # SCI二区可行性
    exaggeration_risk: bool = True     # 是否有夸大风险
    missing_implementation: bool = True # 是否缺少实现细节
    patent_scope_risk: bool = True     # 专利保护范围风险

    @property
    def score(self) -> float:
        risks = [self.sci_feasibility, self.exaggeration_risk,
                 self.missing_implementation, self.patent_scope_risk]
        return 1.0 - sum(risks) / len(risks)
```

**综合评分算法**：

```python
def check_convergence(self, gpt_text: str, gemini_text: str) -> ConvergenceResult:
    # 1. 语义相似度
    semantic_sim = self.calculate_similarity(gpt_text, gemini_text)

    # 2. 结构化合意分数
    structured = self._compute_structured_agreement(gpt_text, gemini_text)

    # 3. 风险消解分数
    risk = self._compute_risk_resolution(gpt_text, gemini_text)

    # 综合评分：权重分配
    final_score = 0.4 * semantic_sim + 0.4 * structured.score + 0.2 * risk.score

    # 提取关键分歧
    disputes = self._extract_disputes(gpt_text, gemini_text)
    critical = [d for d in disputes if d.severity == "critical" and not d.resolved]

    # 人工审核触发条件
    need_human = len(critical) > 0 or final_score < 0.7

    return ConvergenceResult(
        consensus=self._merge_plans(gpt_text, gemini_text, final_score),
        consistency_score=final_score,
        semantic_similarity=semantic_sim,
        structured_agreement=structured,
        risk_resolution=risk,
        need_human_review=need_human,
        critical_unresolved=critical,
        all_disputes=disputes
    )
```

**分歧点自动提取**：

```python
def _extract_disputes(self, gpt_text: str, gemini_text: str) -> list[DisputePoint]:
    disputes = []

    # 检测创新点数量差异（差异≥2为显著分歧）
    gpt_innov = sum(1 for kw in self.KEYWORDS["innovation"] if kw in gpt_text)
    gemini_innov = sum(1 for kw in self.KEYWORDS["innovation"] if kw in gemini_text)
    if abs(gpt_innov - gemini_innov) >= 2:
        disputes.append(DisputePoint(
            topic="创新点数量差异",
            gpt_position=f"GPT提出 {gpt_innov} 个创新点",
            gemini_position=f"Gemini提出 {gemini_innov} 个创新点",
            severity="high"
        ))

    # 检测实验证据缺失（critical级别）
    has_evidence = lambda t: any(kw in t for kw in self.KEYWORDS["evidence"])
    if has_evidence(gpt_text) != has_evidence(gemini_text):
        disputes.append(DisputePoint(
            topic="实验证据缺失",
            gpt_position="有实验证据" if has_evidence(gpt_text) else "无实验证据",
            gemini_position="有实验证据" if has_evidence(gemini_text) else "无实验证据",
            severity="critical"
        ))

    return disputes
```

#### 2.2.4 LangGraph状态机 (DebateGraph)

基于LangGraph实现的状态流转：

```python
class DebateGraph:
    def _should_continue(self, state: DebateState) -> Literal["gemini_node", "gpt_review", "converge_node", END]:
        """路由决策函数"""
        # 轮次耗尽
        if state["round"] >= settings.debate_rounds:
            return "converge_node"

        # 一致性已达标
        score = state.get("consistency_score", 0.0)
        if score >= settings.consistency_threshold:
            return "converge_node"

        # 轮次交替
        if state["round"] == 0:
            return "gemini_node"
        elif state["round"] % 2 == 1:
            return "gpt_review"
        else:
            return "gemini_node"

    def build(self) -> StateGraph:
        """构建状态机图"""
        g = StateGraph(DebateState)

        g.add_node("init", self._init_debate)
        g.add_node("gemini_node", self._gemini_node)
        g.add_node("gpt_review", self._gpt_review_node)
        g.add_node("converge_node", self._converge_node)

        g.set_entry_point("init")

        # 条件边
        for node in ["init", "gemini_node", "gpt_review"]:
            g.add_conditional_edges(
                node,
                self._should_continue,
                {
                    "gemini_node": "gemini_node",
                    "gpt_review": "gpt_review",
                    "converge_node": "converge_node",
                    END: END
                }
            )

        g.add_edge("converge_node", END)
        return g
```

**状态流转图**：

```
                    ┌──────────────────┐
                    │       init       │
                    │  (初始化状态)    │
                    └────────┬─────────┘
                             │
                             ▼
                    ┌──────────────────┐
                    │   gemini_node   │
                    │  (Gemini发散)    │◄── 循环
                    └────────┬─────────┘     │
                             │               │
              ┌──────────────┴──────────────┐
              │  一致性分数 ≥ 阈值？         │
              └──────────────┬──────────────┘
                    ┌────────┴─────────┐
                    │                 │
                   Yes                No
                    │                 │
                    ▼                 ▼
          ┌─────────────┐   ┌──────────────────┐
          │converge_node│   │    gpt_review    │
          │  (收敛结束) │   │   (GPT裁判审查)   │
          └──────┬──────┘   └────────┬─────────┘
                 │                   │
                 │                   └──────────────┐
                 │                              (round++)
                 ▼                                   │
          ┌─────────────┐                            │
          │     END     │◄───────────────────────────┘
          └─────────────┘
```

#### 2.2.5 安全执行器 (ClaudeExecutor)

带有完整安全壳的代码执行器：

```python
class ClaudeExecutor:
    # 安全约束配置
    ALLOWED_EXTENSIONS = {".py", ".md", ".txt", ".json", ".yaml", ".yml",
                          ".csv", ".ipynb", ".sh", ".bat"}
    FORBIDDEN_PATTERNS = ["..", "/etc/", "/root/", "/home/", ".env", ".ssh/",
                          "/usr/bin/", "/usr/local/bin/", "password", "secret"]

    def _validate_path(self, path: str) -> bool:
        """路径安全验证"""
        resolved = Path(path).resolve()
        # 检查是否在workspace内
        try:
            resolved.resolve().relative_to(self.workspace_dir)
        except ValueError:
            return False

        # 检查禁止模式
        for pattern in FORBIDDEN_PATTERNS:
            if pattern in str(resolved):
                return False
        return True

    async def execute_task(self, task: str, task_id: str, timeout: int = None, retry_count: int = 0) -> dict:
        """带重试限制的任务执行"""
        max_retries = settings.claude_code_max_retries

        # 超过重试限制
        if retry_count > max_retries:
            return {"success": False, "error": f"Max retries ({max_retries}) exceeded"}

        # 执行Claude Code命令
        cmd = ["claude", "-p", full_prompt, "--no-input"]

        try:
            proc = await asyncio.create_subprocess_exec(...)
            stdout, stderr = await asyncio.wait_for(proc.communicate(), timeout=timeout)
        except asyncio.TimeoutError:
            # 超时不重试
            return {"success": False, "error": f"Task timeout (>{timeout}s)"}

        # 自动重试：仅针对资源错误/超时
        if not success and retry_count < max_retries:
            if "resource" in stderr_text.lower() or "rate_limit" in stderr_text.lower():
                await asyncio.sleep(2 ** retry_count)  # 指数退避
                return await self.execute_task(task, task_id, timeout, retry_count + 1)

        return result
```

#### 2.2.6 Provider注册与Fallback机制

```python
class FallbackChain(BaseProvider):
    """多Provider级联故障转移"""

    async def generate(self, messages: list[ModelMessage], **kwargs) -> ModelResponse:
        last_error = None

        for provider in self._chain:
            if not provider.enabled:
                continue

            try:
                result = await provider.generate(messages, **kwargs)
                if result.ok:
                    # 记录实际使用的provider
                    result.metadata["fallback_used"] = provider.name
                    return result
                else:
                    last_error = result.error
            except Exception as e:
                last_error = str(e)
                continue

        return ModelResponse(
            error=f"[FallbackChain] All providers failed. Last error: {last_error}"
        )
```

### 2.3 方法流程图

```
用户输入研究主题
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│                    辩论引擎初始化                         │
│  - 解析参与者列表  - 分配角色  - 初始化状态                  │
└─────────────────────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────┐
│              Round 0: Gemini 初始发散                    │
│  - 使用高温(0.9)生成 - 提出创新性方案                      │
│  - 存储到gemini_arguments[0]                              │
└─────────────────────────────────────────────────────────┘
         │
         ▼
    ┌────────────────────────────────┐
    │     收敛检查 (每轮后执行)        │
    │  1. 语义相似度                  │
    │  2. 结构化合意分数              │
    │  3. 风险消解分数                │
    │  4. 综合评分 ≥ 0.85?           │
    └────────────────────────────────┘
         │
    ┌────┴────┐
    │  达标   │  未达标
    │  │     │   │
    │  ▼     │   ▼
    │ 收敛   │  ┌─────────────────────────────────────────┐
    │       │  │  Round N: GPT 裁判审查                    │
    └───────┘  │  - 使用低温(0.3) - 指出问题 - 要求证据      │
              │  - 存储到gpt_arguments[N-1]                │
              └─────────────────────────────────────────┘
                          │
                          ▼
              ┌─────────────────────────────────────────┐
              │  Round N+1: Gemini 创新回应              │
              │  - 针对GPT质疑进行辩护或修正              │
              │  - 补充新的创新点                         │
              └─────────────────────────────────────────┘
                          │
                          ▼
                    ┌────────────┐
                    │ 轮次 < R?  │
                    └────────────┘
                      │      │
                     Yes     No
                      │      │
                      │      ▼
                      │   收敛节点
                      │      │
                      ▼      ▼
                继续辩论    ┌─────────────────────────┐
                            │  生成执行计划           │
                            │  - 提取共识点            │
                            │  - 识别分歧点            │
                            │  - 构建安全执行计划      │
                            └─────────────────────────┘
                                      │
                                      ▼
                            ┌─────────────────────┐
                            │  Claude执行 (可选)   │
                            │  - 任务分解          │
                            │  - 安全执行          │
                            │  - 结果验证          │
                            └─────────────────────┘
                                      │
                                      ▼
                            ┌─────────────────────┐
                            │  轨迹持久化          │
                            │  - 保存辩论记录      │
                            │  - 索引便于检索      │
                            └─────────────────────┘
                                      │
                                      ▼
                              输出最终方案
```

---

## 三、核心创新点（具体算法/策略）

### 3.1 创新点一：角色驱动的多模型认知协同机制

**问题**：现有技术中多模型协同多为简单串联或投票，无法实现真正的"认知碰撞"。

**解决方案**：
1. 定义五种认知角色（ARCHITECT, CRITIC, EXECUTOR, RESEARCHER, JUDGE）
2. 通过Prompt工程将认知角色映射到具体模型
3. 采用交替轮换机制：发散→审查→回应→再审查

**核心代码**：
```python
def get_default_role_map() -> Dict[str, DebateRole]:
    return {
        "gemini": DebateRole.ARCHITECT,  # 负责创新发散
        "gpt": DebateRole.CRITIC          # 负责逻辑审查
    }
```

**技术效果**：
- 不同模型承担互补的认知功能
- 通过角色对立产生真正的思维碰撞
- 温度差异化配置（Gemini 0.9, GPT 0.3）确保角色特性

### 3.2 创新点二：三段式多维收敛评估算法

**问题**：单一相似度指标无法全面评估学术方案的一致性。

**解决方案**：建立三层评估体系
1. **语义层**：使用SentenceTransformer计算文本语义相似度
2. **结构层**：从创新点、证据、专利、实现四个维度检查关键词重叠
3. **风险层**：评估SCI可行性、夸大风险、实现完整性、专利保护

**核心代码**：
```python
def check_convergence(self, gpt_text: str, gemini_text: str) -> ConvergenceResult:
    semantic_sim = self.calculate_similarity(gpt_text, gemini_text)
    structured = self._compute_structured_agreement(gpt_text, gemini_text)
    risk = self._compute_risk_resolution(gpt_text, gemini_text)

    # 权重分配：语义40% + 结构40% + 风险20%
    final_score = 0.4 * semantic_sim + 0.4 * structured.score + 0.2 * risk.score

    need_human = len(critical) > 0 or final_score < 0.7
    return ConvergenceResult(...)
```

**技术效果**：
- 综合评分能准确反映方案一致性
- 自动识别critical级别的未解决分歧
- 触发人工审核的条件明确可控

### 3.3 创新点三：基于关键问题关键词的分歧点自动提取

**问题**：现有技术无法自动识别辩论中的关键分歧点。

**解决方案**：
1. 预定义五类关键问题关键词（创新、证据、专利、实现、SCI级别）
2. 对双方内容进行关键词计数比较
3. 当差异超过阈值时，生成结构化DisputePoint

**核心代码**：
```python
KEYWORDS = {
    "innovation": ["创新", "novel", "创新点", "突破", "首次", "新方法"],
    "evidence": ["实验", "数据", "验证", "证明", "实验设计", "ablation"],
    "patent_scope": ["专利", "保护", "权利要求", "实施例", "覆盖范围"],
    "implementation": ["代码", "实现", "算法", "架构", "模块", "接口"],
    "sci_level": ["SCI", "二区", "发表", "论文", "期刊"],
}

def _extract_disputes(self, gpt_text: str, gemini_text: str) -> list[DisputePoint]:
    # 创新点数量差异检测
    gpt_innov = sum(1 for kw in self.KEYWORDS["innovation"] if kw in gpt_text)
    gemini_innov = sum(1 for kw in self.KEYWORDS["innovation"] if kw in gemini_text)
    if abs(gpt_innov - gemini_innov) >= 2:
        disputes.append(DisputePoint(
            topic="创新点数量差异",
            gpt_position=f"GPT提出 {gpt_innov} 个创新点",
            gemini_position=f"Gemini提出 {gemini_innov} 个创新点",
            severity="high"  # 高级别分歧
        ))

    # 实验证据有无检测（critical级别）
    has_evidence = lambda t: any(kw in t for kw in self.KEYWORDS["evidence"])
    if has_evidence(gpt_text) != has_evidence(gemini_text):
        disputes.append(DisputePoint(
            topic="实验证据缺失",
            severity="critical"  # 严重分歧
        ))
```

**技术效果**：
- 自动追踪辩论过程中的关键分歧
- 分级管理（low/medium/high/critical）
- 为人工审核提供明确关注点

### 3.4 创新点四：带安全壳的工程落地执行

**问题**：学术方案转化为代码时缺乏安全保障。

**解决方案**：
1. 路径验证：确保所有文件操作在workspace内
2. 扩展名过滤：仅允许学术相关文件类型
3. 命令白名单：仅允许pytest等安全命令
4. 执行前后的git diff追踪

**核心代码**：
```python
class ClaudeExecutor:
    FORBIDDEN_PATTERNS = ["..", "/etc/", "/root/", "/home/", ".env", ".ssh/",
                          "/usr/bin/", "password", "secret"]

    def _validate_path(self, path: str) -> bool:
        resolved = Path(path).resolve()
        try:
            resolved.resolve().relative_to(self.workspace_dir)
        except ValueError:
            return False
        for pattern in FORBIDDEN_PATTERNS:
            if pattern in str(resolved):
                return False
        return True
```

**技术效果**：
- 防止恶意代码执行和敏感信息泄露
- 完整的操作审计追踪
- 失败重试机制（指数退避）

### 3.5 创新点五：CDP直连浏览器网页模拟

**问题**：使用Playwright等工具控制浏览器开销大且不灵活。

**解决方案**：
1. 直接通过WebSocket连接Chrome DevTools Protocol
2. 预定义主流AI网站的CSS选择器配置
3. 实现Tab抽象封装常见操作

**核心代码**：
```python
WEBSITE_SELECTORS = {
    "chat.openai.com": {
        "input": "textarea",
        "response": '[data-testid="turn"] .markdown',
        "send_btn": '[data-testid="send-button"]',
    },
    "gemini.google.com": {
        "input": "div[contenteditable='true']",
        "response": ".message-content",
    },
}

class Tab:
    async def type_text(self, selector: str, text: str, delay_ms: int = 50):
        for char in text:
            await self.cdp.send("Input.insertText", {"text": char})
            await asyncio.sleep(delay_ms / 1000)

    async def press_enter(self):
        await self.cdp.send("Input.dispatchKeyEvent", {"type": "keyDown", "key": "Enter"})
```

**技术效果**：
- 零API成本使用网页版AI
- 更精细的浏览器控制
- 自动适配多种AI网站

---

## 四、权利要求书

### 独立权利要求

**权利要求1**：
一种基于多模型辩论协同的学术研究自动化方法，其特征在于，包括以下步骤：

(a) 接收用户输入的研究主题或学术问题；
(b) 初始化多个参与辩论的模型实例，分别为第一模型和第二模型；
(c) 所述第一模型以第一温度参数生成发散性研究方案；
(d) 所述第二模型以第二温度参数对所述发散性研究方案进行学术审查，生成审查意见；
(e) 将所述审查意见反馈给所述第一模型，所述第一模型生成回应或修正方案；
(f) 重复步骤(d)至(e)直到达到预设的辩论轮数或一致性评估达标；
(g) 基于多维度评估算法计算所述第一模型和第二模型的最终输出一致性分数；
(h) 当所述一致性分数达到预设阈值时，生成融合方案和执行计划。

**权利要求2**：
一种实现权利要求1所述方法的系统，包括：
- 辩论引擎模块，用于管理多轮辩论流程和状态流转；
- 角色分配模块，用于为不同模型分配认知角色；
- 收敛评估模块，包括语义相似度计算单元、结构化合意评分单元和风险消解评分单元；
- 分歧追踪模块，用于自动提取和分级管理辩论过程中的关键分歧点；
- 执行计划生成模块，用于生成带有安全约束的可执行计划；
- 轨迹存储模块，用于持久化保存完整辩论过程。

**权利要求3**：
一种计算机可读存储介质，其上存储有程序代码，当所述程序代码被处理器执行时实现权利要求1所述方法的步骤。

### 从属权利要求

**权利要求4**：
根据权利要求1所述的方法，其特征在于，所述第一温度参数为0.7至1.0之间，所述第二温度参数为0.1至0.4之间。

**权利要求5**：
根据权利要求1所述的方法，其特征在于，所述多维度评估算法包括：
- 使用预训练的句子嵌入模型计算两个文本的语义余弦相似度；
- 从创新点、证据完整性、专利保护范围和实现可行性四个维度检查关键词重叠度；
- 评估SCI可行性、夸大风险、实现完整性和专利保护风险。

**权利要求6**：
根据权利要求1所述的方法，其特征在于，当检测到双方在实验证据维度存在分歧时，将该分歧标记为critical级别，并触发人工审核。

**权利要求7**：
根据权利要求1所述的方法，其特征在于，还包括：基于LangGraph构建状态机图，根据辩论轮次和一致性分数动态决定下一步流转方向。

**权利要求8**：
根据权利要求2所述的系统，其特征在于，所述执行计划生成模块还包括路径验证单元，用于验证执行路径是否在预设的workspace目录内，并过滤禁止的路径模式。

**权利要求9**：
根据权利要求2所述的系统，其特征在于，还包括：CDP浏览器控制模块，用于通过Chrome DevTools Protocol直接控制浏览器，实现零成本的网页自动化。

**权利要求10**：
根据权利要求2所述的系统，其特征在于，所述轨迹存储模块包括JSON文件存储单元和SQLite索引单元，分别用于保存完整轨迹和提供快速检索。

**权利要求11**：
根据权利要求1所述的方法，其特征在于，在步骤(h)之后还包括：使用Claude Code执行器执行所述执行计划，并根据安全约束进行路径验证和命令白名单过滤。

**权利要求12**：
根据权利要求1所述的方法，其特征在于，所述认知角色包括架构设计者、风险审查者、落地工程师、资料分析者和最终裁判五种类型。

---

## 五、具体实施方式

### 5.1 实施例一：SCI论文辅助写作

**场景**：用户希望研究"基于深度学习的医学图像诊断"这一主题。

**执行流程**：

1. **初始化**：系统创建Gemini Agent（ARCHITECT角色）和GPT Agent（CRITIC角色）

2. **Round 0 - Gemini发散**：
   ```
   输入：基于深度学习的医学图像诊断
   Gemini输出（temperature=0.9）：
   【架构方案】
   1. 提出使用Transformer架构替代传统CNN
   2. 创新点：引入注意力机制到医学图像分割
   3. 计划使用公开数据集进行验证
   ```

3. **Round 1 - GPT审查**：
   ```
   GPT输出（temperature=0.3）：
   【风险审查】
   1. Transformer在医学图像领域已有相关工作，创新点描述不够具体
   2. 需要提供具体的实验设计和对照组设置
   3. 医学图像数据集的伦理审批是否完成？
   4. 建议补充消融实验设计
   ```

4. **Round 2 - Gemini回应**：
   ```
   Gemini回应（temperature=0.9）：
   【创新发散】
   1. 补充：具体创新在于提出双重注意力机制（通道+空间）
   2. 承诺使用BraTS2023数据集，已获伦理审批
   3. 补充消融实验：Ablation on attention mechanisms
   ```

5. **收敛评估**：
   ```python
   semantic_sim = 0.78
   structured_score = 0.75  # 创新点和实验设计一致
   risk_score = 0.8        # SCI可行性高
   final_score = 0.4*0.78 + 0.4*0.75 + 0.2*0.8 = 0.772

   # 无critical级别分歧
   converged = True
   ```

6. **生成执行计划**：
   ```python
   ExecutionPlan(
       title="医学图像诊断研究 - 执行计划",
       steps=[
           "准备BraTS2023数据集",
           "实现双重注意力Transformer",
           "训练基线CNN模型",
           "消融实验对比"
       ],
       constraints=[
           "需伦理审批文件",
           "需详细记录实验超参数"
       ],
       allowed_files=["src/", "data/", "results/"],
       forbidden_files=[".env", "data/raw_credentials/"]
   )
   ```

### 5.2 实施例二：专利技术方案生成

**场景**：用户提交"一种智能家居节能系统"的技术问题。

**执行流程**类似实施例一，重点检查专利保护范围维度**：
   ```
   patent_scope检测：
   - 双方都提到了"专利保护范围"
   - GPT提出"权利要求书应包含独立和从属权利要求"
   - Gemini提出"实施例应覆盖多种场景"
   patent_scope_aligned = True
   ```

### 5.3 实施例三：人味化降重

**场景**：将AI生成的论文文本改写为更自然的学术写作风格。

**改写策略**：
- 句式重组：主动句↔被动句交替
- 逻辑重排：调整段落顺序
- 风格注入：加入人类写作习惯
- 学术润色：增加hedging表达

---

## 六、附图说明

（此处应包含系统架构图、流程图、状态机图等）

**图1**：AutoSynth-Bridge系统总体架构图
**图2**：三模型辩论协同流程图
**图3**：三段式收敛评估算法流程图
**图4**：LangGraph状态机状态流转图
**图5**：安全执行器验证流程图
**图6**：CDP浏览器控制模块结构图

---

## 七、技术效果总结

| 技术效果 | 现有技术 | 本发明 | 提升 |
|----------|----------|--------|------|
| 创新性 | 单模型生成 | 多模型碰撞 | 创新点更丰富 |
| 严谨性 | 随机审查 | 角色化审查 | 逻辑漏洞更少 |
| 评估维度 | 单一相似度 | 三段式评估 | 评估更全面 |
| 分歧识别 | 人工发现 | 自动提取分级 | 效率提升 |
| 执行安全 | 无约束 | 安全壳保护 | 零风险执行 |
| API成本 | 高频调用 | CDP模拟+降级 | 成本大幅降低 |

---

*交底书版本：V1.0*
*撰写日期：2024年*
*发明人：AutoSynth-Bridge开发团队*